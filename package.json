{
  "name": "lru",
  "version": "1.0.0",
  "description":
    "A least recently used (LRU) cache implementation using Node.js and Express for web accessibility.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel ./src -d ./build",
    "watch": "babel ./src -d ./build --watch",
    "start": "node build/app.js"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1"
  },
  "dependencies": {
    "express": "^4.16.2",
    "uuid": "^3.2.1"
  }
}
