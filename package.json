{
  "name": "lru",
  "version": "1.0.0",
  "description":
    "A least recently used (LRU) cache implementation using native Map in Node.js and Polka for web accessibility.",
  "main": "build/app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel ./src -d ./build -s",
    "watch": "babel ./src -d ./build --watch -s",
    "start": "node build/app.js",
    "postinstall": "npm run build"
  },
  "author":
    "Christian Gaetano <hello@christiangaetano.com> (https://christiangaetano.com)",
  "repository": {
    "type": "git",
    "url": "https://github.com/cgatno/least-recently-used"
  },
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1"
  },
  "dependencies": {
    "body-parser": "^1.18.2",
    "polka": "^0.3.0",
    "uuid": "^3.2.1"
  }
}
